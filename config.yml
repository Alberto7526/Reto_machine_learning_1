estimator:
  - name: average-charges-extractor
    params: {}
  - name: average-charges-regressor # temporalmente dejé este para tener una referencia
    params: {}

data:
  filepath: "./data/train_regression.csv"

metrics:
  - name: mean absolute error # same as above
    params: {}

export:
  output_dir: ./models

reports:
  dir: ./reports